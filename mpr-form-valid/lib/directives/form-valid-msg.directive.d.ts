import { FormValidMsgService } from '../services/form-valid-msg.service';
export declare class FormValidMsgDirective {
    private msgServ;
    validMsg: any;
    constructor(msgServ: FormValidMsgService);
}
