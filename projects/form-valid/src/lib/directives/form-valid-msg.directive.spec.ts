import { FormValidMsgDirective } from './form-valid-msg.directive';

describe('FormValidMsgDirective', () => {
  it('should create an instance', () => {
    const directive = new FormValidMsgDirective();
    expect(directive).toBeTruthy();
  });
});
