import { IsbnPartValidDirective } from './isbn-part-valid.directive';

describe('IsbnPartValidDirective', () => {
  it('should create an instance', () => {
    const directive = new IsbnPartValidDirective();
    expect(directive).toBeTruthy();
  });
});
