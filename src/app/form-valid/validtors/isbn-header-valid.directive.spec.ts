import { IsbnHeaderValidDirective } from './isbn-header-valid.directive';

describe('IsbnHeaderValidDirective', () => {
  it('should create an instance', () => {
    const directive = new IsbnHeaderValidDirective();
    expect(directive).toBeTruthy();
  });
});
