import { IsbnValidtorDirective } from './isbn-validtor.directive';

describe('IsbnValidtorDirective', () => {
  it('should create an instance', () => {
    const directive = new IsbnValidtorDirective();
    expect(directive).toBeTruthy();
  });
});
